name: SDK version cron job

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  fetch_sdk_data:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Get all SDK versions
      run: ./.github/scripts/check-sdk-versions.sh
